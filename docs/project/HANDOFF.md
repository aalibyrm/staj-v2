# Compact Handoff

- Repository state: `origin -> https://github.com/aalibyrm/staj-v2.git`; branch `main` targets `origin/main`; P08-W02 is verified, committed, and pushed at `a482cad7f8cc1a37463412a5a12a4945408589cd`; working tree is clean and synchronized.
- Architecture: `features/analytics` starts here. `models/mastery.models.ts` holds frozen `MasteryAttempt`/`MasteryOutcomeScore`, `MASTERY_BANDS`, typed `MasteryError` codes, and `masteryBandFor`. `domain/mastery-calculation.ts` is a pure documented selector layer: `MASTERY_DEFAULTS`, validated `resolveMasteryOptions`, `selectOutcomeMastery`, `selectOutcomeMasteryById`, and `selectMasteryByOutcomeId`. It imports only types from question-bank and learning-domain and pulls in no Angular, RxJS, transport, or storage.
- Formula (ADR-017): newest 10 attempts per outcome, ordered by `answeredAt` desc then `questionId` asc; weight = `1/(1+0.15*rank)` x difficulty (easy 0.8, medium 1, hard 1.3) x `0.5^priorSightingsOfSameQuestion`; score = weighted mean of `earnedFraction`, clamped and rounded to 4 decimals; bands developing/approaching/proficient/advanced at 0.4/0.6/0.85 with boundaries in the higher band; an outcome with at least one attempt is measured even at zero credit.
- P07-W03: `/learning/dashboard` now uses the established shell and a role/data-scope-aware `ScopedDataFacade`. It composes canonical seed relations, `MockTransport` request scenarios, mastery selectors, and `recommendLearningPathFromAttempts` into readonly KPI, progress, weak/strong outcome, upcoming/activity, and recommendation Signals. The input-driven OnPush `RecommendationReasonCardComponent` renders engine code, summary, detail, and factors.
- UI contract: `general-overview`; the `01-general-overview` brief/reference guided hierarchy and density without copying sample data or the `/overview` visual route. Scoped term/course/cohort/date filters merge URL query parameters. Required loading, slow, empty, service error/retry, unauthorized, and partial-widget states are present; a failed widget does not blank successful regions.
- P07-W04/P07-REV: `/student/:id/analytics` lazy-loads behind exactly student-learning and instructor-teaching capabilities. `StudentAnalyticsFacade` composes seed-domain scope, `MockTransport`, mastery calculation, and the recommendation engine into readonly request, context, filter, KPI, heatmap, trend, recommendation, and support Signals. Students are limited to their own explicit student grant; instructors require an explicit student, cohort, or course grant; program managers and every other role fail closed without individual data. URL-backed filters preserve unrelated query parameters; the trend chart mounts lazily while its accessible table and the heatmap table retain numeric/non-color labels.
- P07-W05: `/cohort-analytics` lazy-loads an instructor/measurement/program-authorized aggregate screen. `selectCohortPrivacy` enforces the inclusive five-learner minimum before copying and freezing individual rows; blocked results expose no rows. `CohortAnalyticsFacade` derives course/cohort scope from canonical grants, rejects foreign filters, preserves unrelated URL query parameters, and composes aggregate KPIs, mastery, attempts, request states, and privacy state through Signals and `MockTransport`.
- P07-W06: `/item-analysis` lazy-loads behind exactly instructor-teaching and measurement-workspace capabilities. `selectItemAnalysis` is a pure immutable selector over published questions plus synchronized learner evidence: facility is mean earned fraction; discrimination is the upper-floor 27% mean minus lower-floor 27% mean with deterministic learner/question ordering; choice options expose count/share/correctness while non-choice items remain explicitly unavailable. `ItemAnalysisFacade` derives exact course scope from canonical grants, rejects foreign URL filters, composes KPI/rows/request state through Signals and `MockTransport`, and keeps mock scenarios inert until refresh/retry.
- P07-W07: `analytics-performance.dataset.ts` adds a deterministic frozen 4,320-record evidence set over the canonical 120 learners, 6 outcomes, and 6 periods. Student/cohort selectors use precomputed finite query caches, return referentially stable frozen slices for repeated keys, reject foreign scope, and preserve the seed graph. `StudentAnalyticsFacade` and `CohortAnalyticsFacade` consume this shared dataset rather than synthesizing attempts per request. The mastery trend remains viewport-deferred with its numeric accessible table.
- Open decisions: none; ADR-007 remains realized as direct `cytoscape@3.34.0`
- P08-W01: the acceptance matrix now maps AC-01..AC-14 to exact executable evidence. The focused Angular gate passed 224/224 tests across 24 files; the complete suite passed 594/594 tests across 55 files; `npx ng build` passed with a 349.83 kB initial bundle and only existing component-style budget warnings. Live checks at 1440x900 and 390x844 verified dashboard/unauthorized rendering, exact-width responsive layout without horizontal overflow, narrow keyboard focus, and no console/page errors. AC-10, AC-11, and AC-14 are now verified; AC-12 remains in progress for later Phase 08 critical-action coverage.
- P08-W02 scope: AC-11 remains one commit boundary. R01 question-bank retry/slow, R02 learning-domain slow plumbing, R03 course catalog, R04 outcome list/map, R05 exam-builder load states, R06 exam-session slow, R07 grading slow, R08 audit-log slow, and R09 closure are complete. Exact allowlists and focused commands remain in `docs/plans/PHASE-08-hardening-delivery.md`.
- P08-W02-R01: Question Bank request state now includes a retryable 400 ms slow transition with revision, terminal, cancellation, supersession, and destroy cleanup. Identical-parameter retry uses `QuestionBankFacade.retry()` rather than re-entering the ordinary deduplicated query path; stale rows and the inspector remain hidden outside success. One stale hidden-inspector expectation required a mechanical test-only correction. The exact R01 focused gate passed 36/36 tests across the facade and component specs.
- P08-W02-R02: `LearningDomainRequestStatus` now includes `slow`; `LearningDomainStore.markRequestSlow` changes only the matching current loading request. `LearningDomainFacade` owns independent 400 ms timers for courses, outcomes, content, and paths, cleared on terminal state, unsubscribe, same-resource supersession, and destruction without cross-resource cancellation. The exact R02 focused gate passed 8/8 tests.
- P08-W02-R03: `/courses` now renders the shared retryable slow state with `aria-busy`, while unauthorized and service errors retain higher precedence and ordinary loading retains its skeleton. Blocking states hide stale course/content rows; authorized-empty and filter-mismatch copy remain distinct. One local request-state fixture union omission required a mechanical correction. The exact R03 focused gate passed 11/11 tests.
- P08-W02-R04: `/outcomes` and `/outcomes/map` now render shared retryable slow states with busy semantics and precedence unauthorized > service load error > slow > loading. Load boundaries reset success flags so blocking states hide stale rows, editor/map details, graph canvas, and accessible list while preserving write-error/conflict feedback on loaded editors and normal graph/list alternatives. Two nonexistent request-state modifier assertions required one mechanical test correction. The exact R04 focused gate passed 26/26 tests across 2 files.
- P08-W02-R05: `/exams/new` resets to a clean draft and `/exams/:id/edit` uses a dedicated revision-safe current-load lifecycle with 400 ms slow, loading, retryable service error, unauthorized-without-retry, and success states independent from automatic selection. New boundaries clear stale exam/history/selection data, timers clear on terminal/cancellation/supersession/destruction, retries preserve the exact id/options, and older pending writes cannot overwrite a newer loaded exam. One malformed unauthorized fixture required a mechanical correction; one diff-discovered pending-write overwrite defect required a behavioral repair. The exact R05 focused gate passed 28/28 tests across 2 files.
- P08-W02-R06: `/exam-session/:token` now adds a revision-safe retryable 400 ms slow state while preserving distinct loading, empty, service error/retry, unauthorized, autosave/offline/conflict, reference-time, and terminal-lock behavior. Exact normalized-token retries recover without stale session/question controls; unauthorized and invalid tokens expose no retry. Slow/reference timers clear on terminal, cancellation, supersession, and destruction, and stale responses cannot replace newer state/data. One nested-observable test fixture required a mechanical correction. The exact R06 focused gate passed 35/35 tests in 1 file.
- P08-W02-R07: `/grading/:attemptId` now adds a revision-safe retryable 400 ms slow state while preserving empty, service error/retry, unauthorized/data-scope denial, score-change confirmation/reason, rollback/history, and workflow behavior. Retries preserve the normalized attempt id and frozen read options; unauthorized, invalid, ready, and empty states expose no retry. Blocking boundaries clear stale grading/form/scoring state, recovered retries rebuild the form, and timers clear on terminal, cancellation, supersession, clear, and destruction. The pre-existing P08-W02 component-spec audit changes remain intact. One missing test type import required a mechanical correction. The exact R07 focused gate passed 35/35 tests across 2 files.
- P08-W02-R08: `/audit-log` now adds a revision-safe retryable 400 ms slow state while preserving loading, authorized empty, service error/recovery, unauthorized/data-scope denial, redaction, query parameters, export, drawer focus, pagination, and responsive table behavior. Blocking loads clear stale records and selection/detail; retry preserves the exact last query; unauthorized and non-retryable terminal states do not issue another request. Timers clear on terminal, cancellation, supersession, and destruction, and stale responses cannot replace current state/data. One filter-mismatched test fixture required a mechanical correction. The exact R08 focused gate passed 19/19 tests across 2 files.
- P08-W02-R09: three stale-callback/write defects required one behavioral repair round; the exam-session component-style error budget required the second. Three readonly/nested-observable/filter fixture failures received minimal mechanical corrections. Final gates passed: 252/252 focused tests across 19 files, 640/640 full-suite tests across 55 files, production build at 350.00 kB with only existing style warnings, and 13 implemented routes at 1440x900 plus 390x844 with no overflow or console/page errors. Narrow focus, exam-session autosave, and role denial/recovery were exercised live. P08-W02 is verified, committed, and pushed at `a482cad7f8cc1a37463412a5a12a4945408589cd`. Next: start P08-W03 accessibility sweep.

Maximum target size: 30 lines. Replace stale facts.
