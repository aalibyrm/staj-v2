# Compact Handoff

- Repository state: `origin -> https://github.com/aalibyrm/staj-v2.git`; branch `main` targets `origin/main`; Phase 06 closed at `09feccb` (`P06-REV`); P07-W07 delivered in `9c2bd61bdd885e0a6450c08c261b13ce2dbc1274`; Phase 07 is closed by `P07-REV`, whose commit SHA is recorded by the current Git gate after push.
- Architecture: `features/analytics` starts here. `models/mastery.models.ts` holds frozen `MasteryAttempt`/`MasteryOutcomeScore`, `MASTERY_BANDS`, typed `MasteryError` codes, and `masteryBandFor`. `domain/mastery-calculation.ts` is a pure documented selector layer: `MASTERY_DEFAULTS`, validated `resolveMasteryOptions`, `selectOutcomeMastery`, `selectOutcomeMasteryById`, and `selectMasteryByOutcomeId`. It imports only types from question-bank and learning-domain and pulls in no Angular, RxJS, transport, or storage.
- Formula (ADR-017): newest 10 attempts per outcome, ordered by `answeredAt` desc then `questionId` asc; weight = `1/(1+0.15*rank)` x difficulty (easy 0.8, medium 1, hard 1.3) x `0.5^priorSightingsOfSameQuestion`; score = weighted mean of `earnedFraction`, clamped and rounded to 4 decimals; bands developing/approaching/proficient/advanced at 0.4/0.6/0.85 with boundaries in the higher band; an outcome with at least one attempt is measured even at zero credit.
- P07-W03: `/learning/dashboard` now uses the established shell and a role/data-scope-aware `ScopedDataFacade`. It composes canonical seed relations, `MockTransport` request scenarios, mastery selectors, and `recommendLearningPathFromAttempts` into readonly KPI, progress, weak/strong outcome, upcoming/activity, and recommendation Signals. The input-driven OnPush `RecommendationReasonCardComponent` renders engine code, summary, detail, and factors.
- UI contract: `general-overview`; the `01-general-overview` brief/reference guided hierarchy and density without copying sample data or the `/overview` visual route. Scoped term/course/cohort/date filters merge URL query parameters. Required loading, slow, empty, service error/retry, unauthorized, and partial-widget states are present; a failed widget does not blank successful regions.
- P07-W04/P07-REV: `/student/:id/analytics` lazy-loads behind exactly student-learning and instructor-teaching capabilities. `StudentAnalyticsFacade` composes seed-domain scope, `MockTransport`, mastery calculation, and the recommendation engine into readonly request, context, filter, KPI, heatmap, trend, recommendation, and support Signals. Students are limited to their own explicit student grant; instructors require an explicit student, cohort, or course grant; program managers and every other role fail closed without individual data. URL-backed filters preserve unrelated query parameters; the trend chart mounts lazily while its accessible table and the heatmap table retain numeric/non-color labels.
- P07-W05: `/cohort-analytics` lazy-loads an instructor/measurement/program-authorized aggregate screen. `selectCohortPrivacy` enforces the inclusive five-learner minimum before copying and freezing individual rows; blocked results expose no rows. `CohortAnalyticsFacade` derives course/cohort scope from canonical grants, rejects foreign filters, preserves unrelated URL query parameters, and composes aggregate KPIs, mastery, attempts, request states, and privacy state through Signals and `MockTransport`.
- P07-W06: `/item-analysis` lazy-loads behind exactly instructor-teaching and measurement-workspace capabilities. `selectItemAnalysis` is a pure immutable selector over published questions plus synchronized learner evidence: facility is mean earned fraction; discrimination is the upper-floor 27% mean minus lower-floor 27% mean with deterministic learner/question ordering; choice options expose count/share/correctness while non-choice items remain explicitly unavailable. `ItemAnalysisFacade` derives exact course scope from canonical grants, rejects foreign URL filters, composes KPI/rows/request state through Signals and `MockTransport`, and keeps mock scenarios inert until refresh/retry.
- P07-W07: `analytics-performance.dataset.ts` adds a deterministic frozen 4,320-record evidence set over the canonical 120 learners, 6 outcomes, and 6 periods. Student/cohort selectors use precomputed finite query caches, return referentially stable frozen slices for repeated keys, reject foreign scope, and preserve the seed graph. `StudentAnalyticsFacade` and `CohortAnalyticsFacade` consume this shared dataset rather than synthesizing attempts per request. The mastery trend remains viewport-deferred with its numeric accessible table.
- Open decisions: none; ADR-007 remains realized as direct `cytoscape@3.34.0`
- Known blocker: none. Phase 07 is complete. P07-REV passed 108/108 focused tests across 13 files, 594/594 full-suite tests across 55 files, and `npx ng build` with only existing style-budget warnings. Live gates at 1440x900 and 390x844 verified dashboard and student analytics hierarchy, request states, recovery, responsive no-overflow layout, keyboard focus, heatmap/trend table alternatives, viewport-deferred trend activation, program-manager student-detail denial, and cohort privacy at four blocked/five allowed with no console or page errors.
- Next: start Phase 08 cross-cutting hardening and delivery

Maximum target size: 30 lines. Replace stale facts.
